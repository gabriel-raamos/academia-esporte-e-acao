{
    "version": 2,
    "builds": [
      { "src": "./", "use": "@vercel/static-build" },
      { "src": "./backend", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/backend/server.js" },
      { "src": "/(.*)", "dest": "/frontend/$1" }
    ]
  }
  